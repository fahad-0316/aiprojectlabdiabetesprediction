# Diabetes_Project

Flask app using KNN to predict diabetes (Pima dataset).

Files to note:
- `app.py` — Flask app
- `train_model.py` — trains model and saves `model.pkl` and `scaler.pkl`
- `templates/index.html` — UI
- `static/style.css` — styles

